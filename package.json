{
  "name": "athletetrainlab-app",
  "version": "1.0.0",
  "private": true,
  "description": "AthleteTrainLab - app cliente (Capacitor wrapper para iOS)",
  "scripts": {
    "build:www": "node scripts/build-www.js",
    "sync:ios": "npm run build:www && npx cap sync ios",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/browser": "^8.0.4",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  }
}
